{
  "name": "WeReader",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "DEBUG=express:* node --use-strict $(node --v8-options | grep harm | awk '{print $1}' | xargs) ./bin/www",
    "test": "node --use-strict $(node --v8-options | grep harm | awk '{print $1}' | xargs) ./test/test"
  },
  "dependencies": {
    "body-parser": "~1.13.2",
    "cookie-parser": "~1.3.5",
    "debug": "~2.2.0",
    "express": "~4.13.1",
    "jade": "~1.11.0",
    "morgan": "~1.6.1",
    "serve-favicon": "~2.3.0"
  },
  "devDependencies": {
    "express-http-proxy": "^0.6.0",
    "http-proxy-middleware": "^0.14.0",
    "lodash": "^4.11.1"
  }
}
